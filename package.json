{
  "name": "lp-art-dr-landing",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/generate-landing-config.js --optional",
    "build:landing": "node scripts/generate-landing-config.js --optional",
    "start": "node server.js"
  }
}
